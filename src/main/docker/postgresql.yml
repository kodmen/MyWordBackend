version: '2'
services:
  mywords-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/MyWords/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=MyWords
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
